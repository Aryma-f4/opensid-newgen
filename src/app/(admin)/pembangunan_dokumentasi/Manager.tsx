"use client"
// CRUD generated by batch generator
import CrudManager from "@/components/admin/CrudManager"

const columns = [
  { key: "keterangan", label: "Keterangan", render: (r: any) => <span className="font-medium">{r.keterangan}</span> },
  { key: "persentase", label: "Persentase" },
]

const fields = [
  { name: "keterangan" as const, label: "Keterangan", type: "text" as const },
  { name: "persentase" as const, label: "Persentase", type: "text" as const },
]

export default function Manager({ initial }: { initial?: any[] }) {
  return (
    <CrudManager
      title="Dokumentasi Pembangunan"
      endpoint="/api/pembangunan_dokumentasi"
      columns={columns}
      fields={fields}
      rowKey={(r) => r.id}
    />
  )
}
